<div class="deck"  id="<%= dom_id deck %>">
  <p class="h3">
    <%= deck.title %>
  </p>
  <p class="text">
    <%= deck.description %>
  </p>
  <div class="a_button">
    <%= link_to 'начать игру', {:action => 'new', :controller => 'rooms', :id => deck.id} %>
  </div>
</div>
